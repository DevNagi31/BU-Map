import { defineConfig } from 'vite';

export default defineConfig({
  // Set base to the subdirectory path if deploying to a non-root URL,
  // e.g. base: '/maps/bu/' for https://example.com/maps/bu/
  base: '/BU-Map/',
});
